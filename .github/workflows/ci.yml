name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint

      - run: pnpm format:check

      # Compiles every .khm example with the published package and fails on any
      # diagnostic the example does not declare — the site must neither ship a picture
      # of faulty wiring nor call a sound diagram faulty.
      - run: pnpm diagrams

      # Both of these write into src/generated, which is not committed, and typecheck reads
      # them. `prebuild` covers a local build; CI type-checks first, so it has to ask.
      - run: pnpm notices

      # The published version and its date, read from npm and the Marketplace.
      - run: pnpm release

      - run: pnpm typecheck

      - run: pnpm build
