/**
 * Where the site is served from.
 *
 * hreflang alternates have to be fully-qualified to be honoured, so the origin cannot stay
 * implicit the way it can in a normal link. Keeping it in one place means a future move
 * changes one line rather than every alternate.
 */
export const ORIGIN = 'https://kumihimo.love-rox.cc';
